/*import Deck from '../classes/deck.js';

export default class Waste {
    constructor(scene) {
        let waste = [];

        if (waste.length > 2) {
            Deck.push(waste);
        }

        this.render = (x, y) => {
            let sprite;
            scene.add.image(x, y, sprite).setScale(1,1);

//            let sprite2;
 //           let sprite3;
// code in draw 1 first then add draw 3 functionality
  //          scene.add.image(x, y, sprite2).setScale(1,1);
  //          scene.add.image(x, y, sprite3).setScale(1,1);

        }
    }

}*/